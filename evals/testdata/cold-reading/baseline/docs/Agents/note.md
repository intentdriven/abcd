# A note under a differently-cased denied namespace

ABCD-EVAL-SENTINEL-DENIED-CASE under a path component that spells a denied
namespace in another case. The structural deny binds every path component, and
the case a directory happens to be spelled in is not a way out of it.
