---
schema_version: 1
id: "iss-268"
slug: "the-attribution-gate-cannot-be-written-about-in-a-fenced-cod"
severity: "minor"
category: "observation"
source: "user-observation"
found_during: "manual-capture"
resolution: "The pull-request-body check strips markdown fenced code blocks before any rule runs, so a fence is quoted material rather than the document own voice. Both ways: a banned footer or co-authorship line inside a fence is an example, and a trailer inside a fence is an example too. BODY ONLY -- the justification is that a forge RENDERS a fence as a code block, and a commit message is never rendered, so the commits arm keeps the unstripped text and the mid-sentence convention. THE DEFENDED PROPERTY is footer-last, not a subset claim: bodies picked up a tool DEFAULT footer across 78 pull requests (itd-91), an accident at scale rather than an adversary, and a tool appends its footer last, which is caught whatever precedes it because no fence opened earlier can still be open at the end without the unterminated rule refusing the whole strip. An earlier draft claimed the stripped region is a SUBSET of what markdown renders as code; that is false and is retracted -- a line-based matcher has no model of markdown block containers. What is not defended is a body CONSTRUCTED AROUND the footer, which was already true since a plainly fenced footer passes by design. Within a document-level context the matcher pairs as CommonMark does (same marker character, at least as long, nothing but whitespace after the run; a backtick opener carrying a backtick in its info string is not an opener), after a security review broke a marker-presence test three ways. An unterminated block strips nothing, which fails closed and makes a longer fence quoting a shorter one work. HTML blocks are excluded outright -- a document containing one strips nothing, safe by construction. Twenty-five corpus cases added (8 accept, 17 reject), including three pinning footer-last, two pinning the HTML guard, and one pinning the list-item residual as a known-accept. Mutation-verified across the pairing clauses, the indent limit, the run length, the unterminated rule, the HTML guard and the commits-arm scoping; three clauses remain unpinned by the corpus and are named in iss-271 rather than claimed. Portable across mawk, one-true-awk and gawk. Residual, iss-270: a fence inside a FIRST-LEVEL list item sits under the absolute indent limit and IS stripped although the forge renders the footer visibly -- under-rejection, the opposite direction from the first draft of that record."
impact: fix
---

The attribution gate cannot be written about in a fenced code block. GENERATED_RE is anchored to line start, so any line inside a markdown code fence that begins with the banned footer shape is refused exactly as a real footer would be -- and a fence is the natural way to document a shape. Pre-existing, not introduced by iss-262: the plain `Generated with [tool](url)` form was already refused inside a fence; iss-262 extends that to the emphasised forms. Hit live on the iss-262 PR itself, whose first body quoted the footer in a fence and failed the gate it tightens. The current convention is to quote the shape mid-sentence instead, which the corpus pins with two accept cases, but that makes the one document most likely to show the shape -- the change that tightens the rule -- the one least able to show it. Options: (a) keep the convention and document it in AGENTS.md so the next author does not rediscover it through a red CI leg; (b) teach the gate fence state, which costs real complexity in a bash security check and opens an evasion route, since a footer wrapped in a fence would go unflagged while still rendering as visible text; (c) accept as-is. Needs a deliberate ruling rather than a same-PR patch.