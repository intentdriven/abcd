---
schema_version: 1
id: "iss-2609012039228906"
slug: "lab-finding-c2-lab-260831131349-976575f-the-generated-abcd-s"
severity: "minor"
category: "observation"
source: "agent-observation"
found_during: "lab-260831131349-976575f"
origin: researcher-authored
production_mode: hand-written
found_at: "commands/consult.md"
resolution: "Declined: the generator is not in this repository. The record binds the lab finding to its real home — the user-scope sources sync script that appends bare pattern lines inside its generated block — and names the in-repo touch point (commands/consult.md, which invokes it) and the sibling defect iss-2608220150157507 (unanchored short patterns, same writer, still open). The fail-safe held: the keyed parser refuses the unparsed lines by line number and the bare banlist render reports them."
impact: internal
---

Lab finding C2 (lab-260831131349-976575f): the generated `abcd-sources` block in the per-machine private banlist store is written by two writers with different ideas of the file's format. abcd scaffolds the store with the keyed declaration on line 1 (the private stub in internal/core/ahoy/banlist_scaffold.go and the header in internal/core/banlist/private.go); the user-scope sources sync script — invoked from commands/consult.md and not part of this repository — then appends bare one-field pattern lines between its fences, which the keyed parser correctly refuses by line number (the fail-safe held, and the bare `abcd banlist` render already reports the format and the malformed line numbers). The lab's reading that one writer leaves both the declaration and the bare lines was inaccurate: two writers, one file. The fix is in the sync script (emit KEY-tab-PATTERN lines, or the declaration itself), outside this repository; the only in-repo touch point is a caveat in commands/consult.md. Cross-link: iss-2608220150157507 is the same writer's other defect (unanchored short patterns) and stays open. Store entries are never quoted here — shapes only.

## Grounds

- declined: the generator is not in this repository; the record binds the lab finding to its real home
