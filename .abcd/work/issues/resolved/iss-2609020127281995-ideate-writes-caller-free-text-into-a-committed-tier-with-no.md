---
schema_version: 1
id: "iss-2609020127281995"
slug: "ideate-writes-caller-free-text-into-a-committed-tier-with-no"
severity: "major"
category: "security"
source: "review-followup"
found_during: "autonomous-run-2026-09-01"
origin: researcher-authored
production_mode: hand-written
found_at: "internal/core/ideate/record.go"
resolution: "internal/core/ideate now imports the shared scanner. A new recordRedactor (internal/core/ideate/redact.go) is built once per run, before the first field is cleaned and long before the first byte is written, and takes intent's FAIL-CLOSED stance rather than capture's redact-and-report one, because ideate writes into .abcd/development/ — the same committed tier intent's draft store writes into — and a design record written under a detector known to be weakened, then reported as clean, is the fail-open the gate exists to close. Stage one redacts every free-text field of the verdict (the idea, each claim and its primary source, each grill note, each kill attempt, each rejected alternative and its reason) on the way into the validated document, ahead of termsafe's cleaning and well ahead of the renderer's escaping, so cell() and blockText() remain the last transformation applied and no placeholder can be dropped into rendered markdown to reopen the structure hazards render.go closes; the enum-constrained members and the cited record ids are deliberately not rewritten. Stage one also applies the deterministic literal-$HOME backstop (SweepCallerHome plus SurvivingCallerHome) that history and memory apply on this same boundary. Stage two re-scans the FULLY RENDERED record body AND the composed decision-log pointer line together, before either is written, and refuses on any blocking residual — so the leak cannot relocate into .abcd/work/DECISIONS.md, whose line stays composed from core-owned values and is verified rather than rewritten (a redacted path would name a record that does not exist). Because the record write is fsutil.CreateExclusiveIn, a one-shot exclusive create, and every refusal returns before it, a degraded scanner leaves no partial document behind. Result gains a Redactions count and the CLI reports it, so a composer learns it just pasted a credential. Proved by TestRecordRedactsSecretsInVerdictProse (a fake ghp_ token in the idea and in a claim plus a set-for-the-test home path: neither literal reaches the note or DECISIONS.md, the fingerprint and the ~ placeholder do, and the idea still says what it said) and TestRecordRefusesOnDegradedScanner (an unparseable .abcd/config/pii.json refuses the run, the research directory is empty and the decision log is byte-identical). OUT OF SCOPE and unchanged: the record FILENAME still carries the caller's slug verbatim, constrained only to lower-case kebab-case — that is the decision-blocked question iss-2609020321100138 holds."
impact: fix
---

ideate writes caller free text into a committed tier with no scanner call — the fifth committed-tier writer the store-before-commit sweep missed. internal/core/ideate/record.go renders the verdict payload and writes it with fsutil.CreateExclusiveIn into .abcd/development/research/, and nothing in internal/core/ideate imports the scanner, so a secret or an identity pasted into the verdict text lands in the durable record in clear while capture, memory, history and intent all route their text through sc.ScanText plus scanner.Redact first. Evidence: internal/core/ideate/record.go (the CreateExclusiveIn call and render), and the absence of any scanner reference in the package. The fix is to route the rendered record through the same shared redactor before the exclusive create, with the stage-two residual check the other stores apply.

## Grounds

- pursued: the tier decides the stance — a writer into .abcd/development/ takes intent's fail-closed posture, and redacting the fields before the renderer rather than the rendered document keeps the escaping authoritative; it would show wrong if a legitimate verdict were refused by the stage-two residual on core-owned bytes the field pass cannot reach, which would mean the slug, not the prose, is the thing that must be rewritten
