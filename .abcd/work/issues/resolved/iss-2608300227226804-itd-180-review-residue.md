---
schema_version: 1
id: "iss-2608300227226804"
slug: "itd-180-review-residue"
severity: "minor"
category: "inconsistency"
source: "impl-review"
found_during: "itd-180 adversarial reviews, 2026-08-30"
found_at: "internal/core/capture/reading.go, internal/core/capture/promote.go, internal/core/lint/readingoutstanding.go"
resolution: "The two standing-disposition readers are held to one answer by a table-driven test and converged on it (a malformed record retires nothing); regime is checked against the regime its position implies and manifest is redacted; promote refuses any standing answer but accepted; a mid-batch write failure names what landed; lint.go's imports are goimports-ordered and IssueStatus's disposition state is documented on the type and the plugin page."
impact: internal
---

itd-180 review residue: capture.standingDispositions and lint.standingDisposition read the standing disposition with different parsers (strict with a continue before the supersedes edge is read; lenient reading the edge always) and no test holds them to one answer, so a malformed sibling disposition makes the board and the verb disagree; at ingest, manifest and regime are written verbatim without redaction or an enum check (harmless until a door lands); promote accepts a rejected or declined reading item; a mid-batch I/O failure leaves earlier items written with no result list; the lint import grouping in lint.go differs from goimports order; IssueStatus carries a disposition state outside the State enum for reading items.
