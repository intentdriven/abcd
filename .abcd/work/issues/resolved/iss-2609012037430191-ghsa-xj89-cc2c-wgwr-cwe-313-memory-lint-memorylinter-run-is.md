---
schema_version: 1
id: "iss-2609012037430191"
slug: "ghsa-xj89-cc2c-wgwr-cwe-313-memory-lint-memorylinter-run-is"
severity: "minor"
category: "security"
source: "agent-finding"
found_during: "autonomous-run-2026-09-01"
origin: researcher-authored
production_mode: hand-written
found_at: "internal/core/memory/lint.go"
resolution: "memory lint now runs the store redactor's read side over every stored page, the sources registry and each text kept-original, emitting a blocker MR001 that names the finding kind and the line and never the span, so report.json and report.md stay clean; a degraded scanner is itself an MR001 blocker against the store path rather than an abort, keeping lint's always-crawl-and-report contract. Lint reports and never rewrites the store (adr-13). TestLintReportsSecretResidueInStoredPages pins the page, sources-index, kept-original, clean-store and degraded-scanner arms; the file-back clone arm landed with the leaf walk as TestFileBackDoesNotPropagateStoredResidue. Out of scope, named as siblings: the derived index.md/contradictions.md/log.md are regenerated by reconcile from the pages, so a page finding covers them, and a binary kept-original cannot be scanned span-wise and is skipped as the write side declines to rewrite it."
impact: fix
---

GHSA-xj89-cc2c-wgwr (CWE-313): memory lint (memoryLinter.run is source classes + licence + quotation) imports no scanner, so a page whose frontmatter or body already holds a PAT or a home path yields zero findings and a clean exit, and fileBackSource deep-copies citation and licence from every matched page onto the file-back page, so a contaminated store propagates itself on ask --file-back (reproduced at v0.7.0: lint --json reports one MS001 info and blockers 0 over a page carrying a PAT in citation.title, recall and the body). The fix must add a blocker-severity MR001 finding built from the store redactor (BlockingResidual over the scan plus the literal-home backstop) over every stored page, .sources_index.json and each text kept-original under sources/ — report, never mutate, per adr-13 — carrying the kind and the line but never the span in the message or the report files, emitting a blocker MR001 naming the store when the scanner is degraded rather than aborting, and the file-back clone must land redacted through the frontmatter walk.

## Grounds

- pursued: the write-time redactor cannot reach text stored before it existed or planted by hand, and a contaminated store propagates itself through every ask --file-back clone, so the read side belongs in the curator health-check that already always crawls the whole store
