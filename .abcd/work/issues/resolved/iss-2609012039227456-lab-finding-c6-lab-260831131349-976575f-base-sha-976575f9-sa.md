---
schema_version: 1
id: "iss-2609012039227456"
slug: "lab-finding-c6-lab-260831131349-976575f-base-sha-976575f9-sa"
severity: "minor"
category: "bug"
source: "agent-observation"
found_during: "lab-260831131349-976575f"
origin: researcher-authored
production_mode: hand-written
found_at: "internal/core/rules/rules.go"
resolution: "Every continuation line of a rule body now renders indented two spaces under its bullet (the markdown list-item continuation): a multi-line rule stays one list item, a continuation beginning with \"- \" cannot forge a sibling rule, and a leading \"#\" cannot forge a heading; the old one-space, hash-only arm is folded into it. A line already indented keeps its deeper indent and a blank line stays blank, so the sanitiser remains a fixed point. The comment now states the threat model the right way round and names the contract defended: the line-start one the host-side parser splits on (\"## \" flush-left), not CommonMark. No bundled default carries a newline, so bundled signatures do not move; a multi-line repo override re-injects once. Proven by TestContinuationLinesStayInsideTheirBullet (watched failing on v0.7.0: got \"## X\\n- a\\nb\\n- c\\n ## d\\n\"), the updated exact-form pin in TestRenderRuleBodyCannotForgeHeadingsOrControlChars, and the fixed-point property in TestSanitizeStableIdempotentAndEditorNeutral."
impact: fix
---

Lab finding C6 (lab-260831131349-976575f, base sha 976575f9): `sanitizeRuleBody` (internal/core/rules/rules.go) indents a continuation line only when it begins with `#`, so every other continuation line of a multi-line rule body renders flush-left and un-bulleted — a multi-line rule reads as a bullet followed by loose paragraphs, a continuation beginning with `- ` forges a sibling rule bullet, and the `#` case gets a single space. Reproduced at v0.7.0 with `abcd rules`. The code comment states the threat model backwards ("only a continuation line would sit flush-left"): every continuation line does, because `Validate` accepts a bare newline and the normaliser turns CR, LS and PS into one. The fix indents every continuation line two spaces (the markdown list-item continuation), keeps the sanitiser a fixed point, and defends the line-start contract the host-side parser splits on (`## ` flush-left); no bundled default carries a newline, so only multi-line repo overrides re-inject once.

## Grounds

- pursued: the defect was the i > 0 arm firing only on "#"; indenting every continuation is the smallest change that keeps a body inside its bullet and keeps the pass idempotent
